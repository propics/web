import {NextRequest,NextResponse} from "next/server";export async function POST(request:NextRequest){const body=await request.json() as Record<string,string>;if(!body.name||!body.email||!body.phone)return NextResponse.json({message:"Please complete all required fields."},{status:400});if(!/^\S+@\S+\.\S+$/.test(body.email))return NextResponse.json({message:"Please enter a valid email address."},{status:400});const webhook=process.env.GOOGLE_APPS_SCRIPT_URL;if(!webhook){if(process.env.BOOKING_DEV_MODE==="true")return NextResponse.json({message:"Trial request validated in local demo mode."});return NextResponse.json({message:"Email service is not connected yet."},{status:503})}const response=await fetch(webhook,{method:"POST",headers:{"content-type":"text/plain;charset=utf-8"},body:JSON.stringify({...body,source:"propics.sa-free-trial",timezone:"Asia/Riyadh"}),redirect:"follow"});if(!response.ok)return NextResponse.json({message:"Request could not be sent."},{status:502});return NextResponse.json({message:"Your free trial request has been sent successfully."})}
